import React, { FC } from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

const MultiplayerRoomScreen: FC = () => {
	return (
		<View>
			<Text>MultiplayerRoomScreen</Text>
		</View>
	);
};

export { MultiplayerRoomScreen };
